version: '3.8'

services:
  nginx:
    build: 
      context: .
      dockerfile: Dockerfile.nginx
    container_name: gnix-rtmp-server
    ports:
      - "1935:1935"    # RTMP port
      - "80:8080"      # HTTP/HLS port (mapped to port 80)
    volumes:
      - hls-data:/var/hls
      - recordings-data:/var/recordings
      - ./www:/var/www
    networks:
      - rtmp-network
    restart: unless-stopped
    environment:
      - NGINX_WORKER_PROCESSES=auto
    depends_on:
      - web

  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    container_name: gnix-dashboard
    ports:
      - "3000:3000"
    volumes:
      - recordings-data:/var/recordings
      - ./public:/app/public
    networks:
      - rtmp-network
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - RTMP_SERVER=${EXTERNAL_IP}
      - PORT=3000

networks:
  rtmp-network:
    driver: bridge

volumes:
  hls-data:
    driver: local
  recordings-data:
    driver: local